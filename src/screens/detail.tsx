import React from 'react';
import {View, Text} from 'react-native';

export interface Props {}

const Detail: React.FC<Props> = ({}: any) => {
  return (
    <View>
      <Text>In Detail screen</Text>
    </View>
  );
};

export default Detail;
